/**
 * Campus timetable display window, shared by the admin schedule builder and
 * the student portal schedule so a slot visible in one is visible in both.
 * Times are campus-local (the backend stores zone-less TimeOnly values).
 */
export const SCHEDULE_START_HOUR = 7;
export const SCHEDULE_END_HOUR = 22;
