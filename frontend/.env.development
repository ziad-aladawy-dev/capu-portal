# Local development defaults. Override locally with .env.development.local
# (gitignored) for machine-specific values.

VITE_API_BASE_URL=http://localhost:5256/api
VITE_WS_URL=ws://localhost:5256/ws
VITE_SENTRY_DSN=
VITE_APP_VERSION=$npm_package_version
VITE_DEPLOY_ENV=development
VITE_ENABLE_MOCKS=true
VITE_DEFAULT_LNG=ar
